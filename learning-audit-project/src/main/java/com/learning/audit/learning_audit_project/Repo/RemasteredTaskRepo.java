package com.learning.audit.learning_audit_project.Repo;

import com.learning.audit.learning_audit_project.Model.RemasteredTask;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RemasteredTaskRepo extends JpaRepository<RemasteredTask, Long> {
}
